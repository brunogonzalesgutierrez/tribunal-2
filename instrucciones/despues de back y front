Rellenado de todo ojo hacer eso con la tablas vacias esto dura 10 minutos en rellenarse

enotra terminal

cd tribunal_backend

python -m venv venv
venv\Scripts\activate

python seed_graphql.py


python manage.py shell

from django.contrib.auth.hashers import make_password
from tribunal.models import Rol, Usuario

rol = Rol.objects.get(nombre="Administrador")

u = Usuario.objects.create(
    nombres="Daniel",
    paterno="Gonzales",
    documento_identidad="12345678",
    email="danielgonzalesgutierrez12@gmail.com",
    username="daniel",
    password=make_password("admin123"),
    rol=rol,
    activo=True
)
